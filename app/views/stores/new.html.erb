<h2 class="title" >新增店家資料</h2>


<%= form_for @store, url:user_stores_path(user_id:current_user.id), method: :post  do |form| %>
    <div class="field">
        <%= form.label :title, "店名", class: "label" %>
        <div class="control">
            <%= form.text_field :title, class: "input" %>
        </div>
    </div>

<% end %>


              
